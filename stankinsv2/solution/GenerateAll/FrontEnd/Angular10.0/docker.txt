FROM node:12.0
ENV NODE_ROOT usr/app/

RUN npm install -g @angular/cli@10.0.4

COPY . ./

RUN npm i

RUN set -x \
    && node -v \
    && npm -v \
    && ng version
	
CMD ng serve --host 0.0.0.0 --port 4200
EXPOSE 4200 