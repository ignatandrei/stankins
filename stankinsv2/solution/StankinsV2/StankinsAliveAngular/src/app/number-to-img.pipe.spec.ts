import { NumberToImgPipe } from './number-to-img.pipe';

describe('NumberToImgPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToImgPipe();
    expect(pipe).toBeTruthy();
  });
});
